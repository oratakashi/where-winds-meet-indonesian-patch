# Phase 6 — idx 66,000–66,999 (batch 10, 2026-09-22)

Part of [[Phase-6]]. Second batch at the **1,000-strings/iteration** size.

Same mix as prior batches: a very high proportion of `freq: 2` random
player-username strings with irregular internal capitalization (kept
verbatim per [[Special-Cases]]/[[Names-Not-Translated]]), Chinese-style NPC
pinyin names, gear/skill UI labels (`Tier N <Set>: <Slot>`, `- EX`/`Recommended:
#Y<Path>#E Path` price-fluctuation flavor lines), stat/skill-scaling
descriptions, and casual gue/lo dialogue per [[Tone-And-Style]]. No new
terminology decisions — every case matched an existing [[Glossary]] entry.

- Honorifics applied correctly on first pass: Young Master → Tuan Muda
  (idx 66251 "my young master"), no Master/Elder/Granny/Uncle edge cases
  needed correction this batch.
- `<...>` tag-content rules: plain non-stat tags kept 100% in English (idx
  66159 `<Real-time Voice in Background Mode>`), stat-formatted
  `<Name|id|#C|n>` tags copied byte-for-byte while translating the
  surrounding sentence (idx 66150, 66219, 66480, 66626, 66802, 66897 —
  multi-tag skill-scaling/talent descriptions).
- Several longer narrative/lore entries translated in full: the Emperor
  Taizu of Song frugality anecdote (idx 66059), the lion-dance
  Liangzhou-to-Chang'an history note (idx 66187), the Buddha Statue "true
  spell" village-worship lore (idx 66792), the escaped-princess/Yingying
  palace-escape epilogue (idx 66950), and the shoe-making decade-long
  romance item flavor text (idx 66161).
- Duration/placeholder conventions: `s`→`d` shorthand both bare (idx 66089
  `{second}s`→`{second}d`) and inside tags/cooldown lines (idx 66544
  `Cooldown: 80s`→`80d`, plus `#Y8s#E`→`#Y8d#E` in the same entry; idx
  66621 `10s`→`10d`); `$S...$E` narrative-wrapper handling with translated
  content inside (idx 66441); `$STEADY_MIN_PRO_ATK_C:.1f$` and
  `$T(2019)`/`$N`/`$H`/`$link<...>^ID^$` pure runtime placeholders
  preserved character-for-character (idx 66480, 66438, 66752, 66944,
  66506).

Translated in two ~500-row sub-batches (scratch files via the `Write`
tool, no shell heredoc involved, avoiding any `$`-escaping risk) before
merging and validating.

Token/placeholder validation ran on the full 1,000-row batch via the
standard `TOKEN` regex script: **3 mismatches found and fixed**:
- idx 66159 — a plain (non-stat) `<...>` tag was mistakenly translated
  (`<Suara Real-time dalam Mode Latar Belakang>`); corrected to keep the
  tag's English content untouched per the plain-tag rule.
- idx 66626 — the source tag `<Nameless Sword's|781|#C|10102>` (note the
  `'s`) was copied as `<Nameless Sword|781|#C|10102>`, dropping the
  apostrophe-s from inside the tag; corrected to match byte-for-byte.
- idx 66897 — the second `#Yresonance#E` occurrence in the source was
  translated as plain untagged text ("resonansi") instead of keeping the
  `#Y...#E` wrapper; corrected to `#Yresonansi#E`.

Re-ran after fixes: **0 mismatches**, 0 EMPTY values. Full merged
`locale/phase6.jsonl` (17,000 lines) re-validated: idx sequential
50,000–66,999, 17,000 unique idx, no duplicates.
